# CI/CD FastAPI AWS Demo

A small FastAPI application used to demonstrate a practical CI/CD workflow with GitHub Actions, Docker, and AWS deployment.
The repo shows how feature branches, pull requests, automated checks, environment variables, and staging/production deployments work together in a real delivery pipeline.
